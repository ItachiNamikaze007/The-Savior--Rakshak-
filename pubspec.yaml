name: sosquad
description: "SoSquad - RAKSHAK-NET Hybrid Disaster Response and Rescue Coordination System"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  geolocator: ^13.0.1
  intl: ^0.19.0
  uuid: ^4.5.1
  google_fonts: ^6.2.1
  firebase_core: ^4.13.0
  cloud_firestore: ^6.8.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
